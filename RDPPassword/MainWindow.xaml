<ui:FluentWindow
    x:Class="RDPPassword.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:RDPPassword"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="RDPPassword"
    Width="600"
    Height="400"
    ExtendsContentIntoTitleBar="True"
    ResizeMode="CanMinimize"
    WindowBackdropType="Mica"
    WindowCornerPreference="Round"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid x:Name="GridRoot" Loaded="GridRoot_Loaded">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            Title="RDPPassword"
            Grid.Row="0"
            Height="36"
            Padding="16,4,8,4"
            CanMaximize="False"
            ShowMaximize="False">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://siteoforigin:,,,/RDPPassword22.ico" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <Grid Grid.Row="1" Margin="8,0,14,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Menu Grid.Column="0" FontSize="16">
                <ui:MenuItem
                    x:Name="ButtonEncrypt"
                    Click="ButtonEncrypt_Click"
                    Header="Encrypt" />
                <ui:MenuItem
                    x:Name="ButtonDecrypt"
                    Click="ButtonDecrypt_Click"
                    Header="Decrypt" />
                <Separator />
                <ui:MenuItem
                    x:Name="ButtonCopy"
                    Click="ButtonCopy_Click"
                    ToolTip="Copy">
                    <ui:MenuItem.Icon>
                        <ui:SymbolIcon x:Name="IconButtonCopy" Symbol="Copy24" />
                    </ui:MenuItem.Icon>
                </ui:MenuItem>
                <ui:MenuItem
                    x:Name="ButtonClear"
                    Click="ButtonClear_Click"
                    Icon="{ui:SymbolIcon Eraser24}"
                    ToolTip="Clear" />
                <Separator />
            </Menu>

            <CheckBox
                x:Name="CheckBoxBase64"
                Grid.Column="2"
                Margin="8,2,0,0"
                VerticalAlignment="Center"
                Content="Base64"
                ToolTip="Base64" />

            <TextBlock
                x:Name="TextBlockInfo"
                Grid.Column="3"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Foreground="Gray"
                Text="[Info]" />
        </Grid>

        <Grid
            x:Name="GridMain"
            Grid.Row="2"
            Margin="0,0,0,0">
            <TextBox
                x:Name="TextBoxMain"
                Margin="12,0,12,12"
                FontFamily="Consolas"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                TextWrapping="Wrap" />

        </Grid>

    </Grid>
</ui:FluentWindow>
